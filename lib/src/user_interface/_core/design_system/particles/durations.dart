class Durations {
  const Durations._();

  static const processingSnackBar = Duration(milliseconds: 500);

  static const successSnackBar = Duration(seconds: 2);

  static const errorSnackBar = Duration(seconds: 3);
}
